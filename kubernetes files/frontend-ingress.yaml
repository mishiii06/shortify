apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shortify-frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: www.shortify.site  # Update with your desired hostname or use a dynamic hostname
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: shortify-frontend-service  # Name of your shortify-backend Kubernetes service
            port:
              number: 80          # Port on which your shortify-backend service is exposed

